# GitHub Preparation Commands (Do Not Run Yet)

```bash
git init
git add .
git commit -m "Prepare project for secure public release"
git remote add origin https://github.com/anikaitar70/<repo-name>.git
git branch -M main
git push -u origin main
```

Replace `<repo-name>` with your target repository name.
